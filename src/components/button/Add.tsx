import { Button } from "@mui/material";

const Add = () => {
  return (
    <Button
      variant="contained"
      sx={{ width: 100, marginRight: 3, fontSize: 16, fontWeight: 700 }}
    >
      추가
    </Button>
  );
};

export default Add;
