import { Box } from "@mui/material";

const IntroPage = () => {
  return (
    <Box
      sx={{
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "28px"
      }}
    >
      좌측의 메뉴를 선택해 주세요
    </Box>
  );
};

export default IntroPage;
